
version: 2.1
orbs:
  node: circleci/node@5.0.2
  aws-cli: circleci/aws-cli@3.0.0

workflows:
  version: 2
  build-and-deploy:
    jobs:
      - frontend-environment
      - backend-environment
      - build-frontend:
          requires:
            - frontend-environment
      - build-backend:
          requires:
            - backend-environment
      - test-frontend:
          requires:
            - build-frontend
      - test-backend:
          requires:
            - build-backend
      - deploy-frontend:
          requires:
            - test-frontend
      - deploy-backend:
          requires:
            - test-backend
            
      
              
jobs:
  frontend-environment:
    docker:
      - image: cimg/base:stable
        auth:
          username: $DOCKERHUB_USER
          password: $DOCKERHUB_PASSWORD
    steps:
      - node/install
      - checkout
      - aws-cli/setup:
          configure-default-region: true 
          configure-profile-region: true 
          disable-aws-pager: true 
          override-installed: false 
          aws-access-key-id: AWS_ACCESS_KEY_ID
          aws-secret-access-key: AWS_SECRET_ACCESS_KEY
          aws-region: AWS_REGION
          profile-name: AWS_PROFILE_NAME
      - save_cache:
          working_directory: /
          command: mkdir /circleci-demo
          key: v1-front-{{ .Environment.CIRCLE_SHA1 }}
          paths:
            - /circleci-demo
  backend-environment:
    docker:
      - image: cimg/base:stable
        auth:
          username: $DOCKERHUB_USER
          password: $DOCKERHUB_PASSWORD
    steps:
      - node/install
      - checkout
      - aws-cli/setup:
          configure-default-region: true 
          configure-profile-region: true 
          disable-aws-pager: true 
          override-installed: false 
          aws-access-key-id: AWS_ACCESS_KEY_ID
          aws-secret-access-key: AWS_SECRET_ACCESS_KEY
          aws-region: AWS_REGION
          profile-name: AWS_PROFILE_NAME        
      - run:
          name: Installing deployment dependencies
          working_directory: /
          command: |
            sudo apt-get -y -qq update
            sudo apt-get install python3-pip python3-dev build-essential
            sudo pip3 install awsebcli
      - save_cache:
          working_directory: /
          command: mkdir /circleci-demo
          key: v1-backend-{{ .Environment.CIRCLE_SHA1 }}
          paths:
            - /circleci-demo
  build-frontend:
    docker:
      - image: cimg/base:stable
        auth:
          username: $DOCKERHUB_USER
          password: $DOCKERHUB_PASSWORD
    steps:     
      - restore_cache:
          keys:
            - v1-front-{{ checksum "Gemfile.lock" }}
      - checkout
      - run:
          name: Front-End Install
          command: npm run frontend:install
      - run:
          name: Front-End build
          command: npm run frontend:build
      - save_cache:
          key: v1-front-build-{{ .Environment.CIRCLE_SHA1 }}
          paths:
            - /circleci-demo
  build-backend:
    docker:
      - image: cimg/base:stable
        auth:
          username: $DOCKERHUB_USER
          password: $DOCKERHUB_PASSWORD
    steps:
      - restore_cache:
          keys:
            - v1-backend-{{ checksum "Gemfile.lock" }}
      - checkout
      - run:
          name: Back-End Install
          command: npm run backend:install
      - run:
          name: Back-End build
          command: npm run backend:build
      - save_cache:
          key: v1-backend-build-{{ .Environment.CIRCLE_SHA1 }}
          paths:
            - /circleci-demo
  test-frontend:
    docker:
      - image: cimg/base:stable
        auth:
          username: $DOCKERHUB_USER
          password: $DOCKERHUB_PASSWORD
    steps:
      - restore_cache:
          keys:
            - v1-front-build-{{ checksum "Gemfile.lock" }}
      - checkout
      - run:
          name: Front-End test
          command: npm run frontend:test:ci
  test-backend:
    docker:
      - image: cimg/base:stable
        auth:
          username: $DOCKERHUB_USER
          password: $DOCKERHUB_PASSWORD
    steps:
      - restore_cache:
          keys:
            - v1-backend-build-{{ checksum "Gemfile.lock" }}
      - checkout
      - run:
          name: Back-End test
          command: npm run backend:test
  deploy-frontend:
    docker:
      - image: cimg/base:stable
        auth:
          username: $DOCKERHUB_USER
          password: $DOCKERHUB_PASSWORD
    steps:
      - restore_cache:
          keys:
            - v1-front-build-{{ checksum "Gemfile.lock" }}
      - checkout
      - run:
          name: Front-End deploy
          command: npm run frontend:deploy
  deploy-backend:
    docker:
      - image: cimg/base:stable
        auth:
          username: $DOCKERHUB_USER
          password: $DOCKERHUB_PASSWORD
    steps:
      - restore_cache:
          keys:
            - v1-backend-build-{{ checksum "Gemfile.lock" }}
      - checkout
      - run:
          name: Back-End deploy
          command: npm run backend:deploy


